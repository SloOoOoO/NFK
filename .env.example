# ===================================================
# NFK Buchhaltung - Environment Variables Template
# ===================================================
# 
# SECURITY WARNING:
# ⚠️  Never commit actual secrets to version control!
# ⚠️  Copy this file to `.env` and fill in real values
# ⚠️  The `.env` file is in .gitignore and will not be committed
#
# This file serves as a template showing all available
# environment variables for the NFK application.
# ===================================================

# ===================================================
# Database Configuration
# ===================================================
SQL_SERVER_PASSWORD=YourStrong!Passw0rd
# Connection string will use: Server=localhost;Database=NFK;User Id=sa;Password=${SQL_SERVER_PASSWORD}

# ===================================================
# JWT Authentication
# ===================================================
# Generate RSA keys with:
# openssl genrsa -out private.pem 2048
# openssl rsa -in private.pem -pubout -out public.pem
# Then set the PEM content (with \n for newlines):
JWT_PRIVATE_KEY=
JWT_PUBLIC_KEY=

# Or use symmetric key (not recommended for production):
# JWT_SECRET=YourVeryLongSecretKeyHere

# ===================================================
# Google OAuth 2.0 Configuration
# ===================================================
# Setup Instructions:
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Create OAuth 2.0 Client ID for Web application
# 3. Add authorized redirect URIs:
#    - Development: http://localhost:8080/api/v1/auth/google/callback
#    - Production: https://your-domain.com/api/v1/auth/google/callback
# 4. Copy Client ID and Client Secret here
# 5. Set GOOGLE_OAUTH_ENABLED=true

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_OAUTH_ENABLED=false

# OAuth URLs (hardcoded in GoogleOAuthService.cs, shown here for reference):
# Authorization: https://accounts.google.com/o/oauth2/v2/auth
# Token: https://oauth2.googleapis.com/token
# UserInfo: https://www.googleapis.com/oauth2/v2/userinfo

# ===================================================
# DATEV OAuth 2.0 Configuration
# ===================================================
# Setup Instructions:
# 1. Contact DATEV at https://developer.datev.de/
# 2. Register your application and request OAuth credentials
# 3. Add authorized redirect URIs:
#    - Development: http://localhost:8080/api/v1/auth/datev/callback
#    - Production: https://your-domain.com/api/v1/auth/datev/callback
# 4. Copy Client ID and Client Secret here
# 5. Set DATEV_OAUTH_ENABLED=true
# Note: Only available for certified tax consultants in Germany

DATEV_CLIENT_ID=
DATEV_CLIENT_SECRET=
DATEV_OAUTH_ENABLED=false

# ===================================================
# DATEV SFTP Configuration
# ===================================================
DATEV_SFTP_HOST=sftp.datev.de
DATEV_SFTP_PORT=22
DATEV_SFTP_USERNAME=
DATEV_SFTP_PASSWORD=

# ===================================================
# Email Configuration
# ===================================================
# SendGrid (recommended for production)
SENDGRID_API_KEY=

# SMTP (alternative to SendGrid)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_ENABLE_SSL=true

# Email provider to use: "SendGrid" or "Smtp"
EMAIL_PROVIDER=Smtp

# ===================================================
# Azure Storage (optional, for cloud document storage)
# ===================================================
AZURE_STORAGE_CONNECTION_STRING=

# ===================================================
# Redis Cache
# ===================================================
REDIS_CONNECTION_STRING=localhost:6379

# ===================================================
# Frontend URL
# ===================================================
FRONTEND_URL=http://localhost:5173

# ===================================================
# Security Settings
# ===================================================
MAX_FAILED_LOGIN_ATTEMPTS=5
ACCOUNT_LOCKOUT_MINUTES=30

# Data encryption key (32+ characters)
ENCRYPTION_MASTER_KEY=

# ===================================================
# Application Settings
# ===================================================
ASPNETCORE_ENVIRONMENT=Development
# Options: Development, Staging, Production

# ===================================================
# Docker Compose Override
# ===================================================
# If using docker-compose, ensure docker-compose.yml
# references these environment variables properly
# ===================================================
